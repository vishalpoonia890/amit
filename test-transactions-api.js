// Test script to verify transactions API endpoints\r\nconst axios = require('axios');\r\n\r\n// Replace with your actual backend URL\r\nconst BASE_URL = 'http://localhost:10000'; // or your Render URL\r\n\r\n// Replace with a valid JWT token from a logged in user\r\nconst TEST_TOKEN = 'YOUR_JWT_TOKEN_HERE';\r\n\r\nasync function testTransactionsAPI() {\r\n  try {\r\n    console.log('Testing transactions API endpoints...\\n');\r\n    \r\n    // Test recharges endpoint\r\n    console.log('1. Testing /api/recharges endpoint...');\r\n    const rechargesResponse = await axios.get(`${BASE_URL}/api/recharges`, {\r\n      headers: {\r\n        'Authorization': `Bearer ${TEST_TOKEN}`\r\n      }\r\n    });\r\n    console.log('Recharges response:', rechargesResponse.data);\r\n    \r\n    // Test withdrawals endpoint\r\n    console.log('\\n2. Testing /api/withdrawals endpoint...');\r\n    const withdrawalsResponse = await axios.get(`${BASE_URL}/api/withdrawals`, {\r\n      headers: {\r\n        'Authorization': `Bearer ${TEST_TOKEN}`\r\n      }\r\n    });\r\n    console.log('Withdrawals response:', withdrawalsResponse.data);\r\n    \r\n    console.log('\\nAPI tests completed successfully!');\r\n  } catch (error) {\r\n    console.error('API test failed:', error.response?.data || error.message);\r\n  }\r\n}\r\n\r\n// Run the test\r\ntestTransactionsAPI();